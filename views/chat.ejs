<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>LawGPT - AI Chat</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    /* --- THEME COLORS --- */
    :root {
      --bg-color: #0b1f3a;        /* Deep Navy Blue */
      --chat-panel: #112d52;      /* Lighter Navy */
      --msg-user: #1f4068;        /* User Bubble */
      --msg-bot: #162447;         /* Bot Bubble */
      --text-primary: #e0e6ed;    /* Off-white */
      --text-muted: #94a3b8;      /* Grey text */
      --accent: #7bbcff;          /* Light Blue */
      --highlight: #60a5fa;       /* Focus Blue */
      --border: #1f4068;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', system-ui, sans-serif;
      background: var(--bg-color);
      color: var(--text-primary);
      display: grid;
      grid-template-rows: auto 1fr auto; /* Header | Chat | Input */
      height: 100vh;
      overflow: hidden; /* Prevent body scroll */
    }

    /* Accessibility Skip Link */
    .skip-link {
      position: absolute;
      top: -40px; left: 0;
      background: var(--highlight); color: #000;
      padding: 8px; z-index: 100;
    }
    .skip-link:focus { top: 0; }

    /* Header */
    header {
      background: var(--chat-panel);
      padding: 1rem;
      border-bottom: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header-left { display: flex; align-items: center; gap: 12px; }
    .logo-img { height: 45px; width: auto; }
    h1 { margin: 0; font-size: 1.25rem; font-weight: 600; color: var(--accent); }

    .help-link {
      color: var(--text-primary);
      text-decoration: none;
      font-size: 0.9rem;
      padding: 6px 12px;
      border: 1px solid var(--accent);
      border-radius: 4px;
    }
    .help-link:hover, .help-link:focus { background: var(--accent); color: var(--bg-color); }

    /* Chat Area */
    main {
      overflow-y: auto;
      padding: 1rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      max-width: 900px;
      margin: 0 auto;
      width: 100%;
      box-sizing: border-box;
      scroll-behavior: smooth;
    }

    .message {
      max-width: 85%;
      padding: 1rem;
      border-radius: 0.75rem;
      line-height: 1.6;
      position: relative;
      word-wrap: break-word;
    }
    
    .message.user {
      align-self: flex-end;
      background: var(--msg-user);
      color: #fff;
      border-bottom-right-radius: 0.1rem;
      border: 1px solid var(--border);
    }
    
    .message.bot {
      align-self: flex-start;
      background: var(--msg-bot);
      color: var(--text-primary);
      border-bottom-left-radius: 0.1rem;
      border: 1px solid var(--border);
    }

    /* Source / Citation Styling */
    .sources-container {
      margin-top: 10px;
      padding-top: 8px;
      border-top: 1px solid rgba(255,255,255,0.1);
      font-size: 0.85rem;
    }
    .source-badge {
      display: inline-block;
      background: rgba(123, 188, 255, 0.15);
      color: var(--accent);
      padding: 2px 8px;
      border-radius: 4px;
      margin-right: 6px;
      margin-top: 4px;
      border: 1px solid rgba(123, 188, 255, 0.3);
    }

    /* Input Area */
    .input-area {
      background: var(--chat-panel);
      padding: 1rem;
      border-top: 1px solid var(--border);
    }

    form {
      display: flex; /* Flexbox to align Input, Send, Mic */
      gap: 10px;
      max-width: 900px;
      margin: 0 auto;
      width: 100%;
      align-items: center;
    }

    /* Mic & Send Buttons */
    .icon-btn {
      background: var(--accent);
      color: var(--bg-color);
      border: none;
      width: 50px;
      height: 50px;
      border-radius: 50%; /* Circle shape */
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      transition: all 0.2s;
      flex-shrink: 0; /* Prevent shrinking */
    }
    .icon-btn:hover { filter: brightness(1.1); transform: scale(1.05); }
    
    /* Input Field */
    input[type="text"] {
      flex: 1; /* Take remaining space */
      padding: 0 1rem;
      border: 1px solid var(--border);
      border-radius: 25px; /* Rounded pill shape */
      font-size: 1rem;
      background: #061221;
      color: #fff;
      height: 48px;
      outline: none;
    }
    input[type="text"]:focus { border-color: var(--highlight); }

    /* Hint Text Styling */
    .hint-text {
      display: block;
      text-align: center;
      color: var(--text-muted);
      font-size: 0.8rem;
      margin-top: 8px;
    }
    kbd {
      background: #1f4068;
      padding: 2px 6px;
      border-radius: 4px;
      color: var(--accent);
      font-family: monospace;
      border: 1px solid var(--border);
    }

    /* Animation for Mic Listening */
    .listening {
      animation: pulse 1.5s infinite;
    }
    @keyframes pulse {
      0% { box-shadow: 0 0 0 0 rgba(255, 68, 68, 0.4); }
      70% { box-shadow: 0 0 0 10px rgba(255, 68, 68, 0); }
      100% { box-shadow: 0 0 0 0 rgba(255, 68, 68, 0); }
    }

  </style>
</head>
<body>

  <a href="#chat-container" class="skip-link">Skip to chat messages</a>

  <header role="banner">
    <div class="header-left">
      <img src="/Images/law-gpt-logo.png" alt="LawGPT Logo" class="logo-img" />
      <h1>LawGPT</h1>
    </div>
    <a href="/about" class="help-link">Help & About</a>
  </header>

  <main id="chat-container" role="log" aria-label="Chat history">
    <div class="message bot">
      <div class="message-content">
        <strong>Namaste! I am LawGPT.</strong><br><br>
        I use the new <strong>BNS 2023</strong> laws by default.<br>
        <ul>
          <li>For new laws: <em>"What is Section 302?"</em></li>
          <li>For old laws (IPC): <em>"What is IPC 302?"</em></li>
        </ul>
        <br>
        <strong>Try the new Voice Mode!</strong> Click the microphone 🎤 below.
      </div>
    </div>
  </main>

  <div class="input-area">
    <form id="chat-form">
      
      <input 
        type="text" 
        id="user-input" 
        placeholder="Type a legal question..." 
        autocomplete="off" 
      />

      <button type="submit" class="icon-btn" title="Send">
        ➤
      </button>

      <button type="button" id="mic-btn" class="icon-btn" title="Speak">
        🎤
      </button>

    </form>

    <span class="hint-text">
        Shortcuts: 
        <kbd>Alt+A</kbd> Mic &middot; 
        <kbd>Ctrl+Enter</kbd> Send &middot; 
        <kbd>Esc</kbd> Stop Audio
    </span>

  </div>

  <script type="module" src="/js/chat.js"></script>

</body>
</html>