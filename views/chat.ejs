<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" /><title>LawGPT - Chat</title>
  <style>
    :root { --bg: #0b1f3a; --panel: #112d52; --text: #e0e6ed; --accent: #7bbcff; }
    body { background: var(--bg); color: var(--text); font-family: sans-serif; display: flex; flex-direction: column; height: 100vh; margin: 0; }
    header { background: var(--panel); padding: 10px; border-bottom: 1px solid #1f4068; display: flex; justify-content: space-between; align-items: center; }
    .hf-bar { padding: 10px; background: #061221; text-align: center; color: var(--accent); font-weight: bold; border-bottom: 1px solid #1f4068; }
    main { flex: 1; overflow-y: auto; padding: 20px; }
    .input-area { background: var(--panel); padding: 20px; }
    form { display: flex; gap: 10px; max-width: 800px; margin: 0 auto; }
    input { flex: 1; padding: 12px; border-radius: 20px; border: none; background: #061221; color: #fff; }
    button { background: var(--accent); border: none; border-radius: 20px; padding: 0 20px; cursor: pointer; font-weight: bold; }
  </style>
</head>
<body>
  <header><h1>LawGPT</h1><a href="/about" style="color:white; text-decoration:none; border:1px solid var(--accent); padding:5px; border-radius:4px;">Settings</a></header>
  <div class="hf-bar"><input type="checkbox" id="hf-toggle"> <label for="hf-toggle">Hands-Free (Say "Hey LawGPT")</label></div>
  <main id="chat-container"></main>
  <div class="input-area">
    <form id="chat-form"><input type="text" id="user-input" placeholder="Ask a question..." autocomplete="off"> <button type="submit">Send</button></form>
  </div>
  <script type="module" src="/js/chat.js"></script>
</body>
</html>
